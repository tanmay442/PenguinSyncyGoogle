[package]
name = "penguinsyncygoogle"
version = "0.1.0"
edition = "2024"
description = "Selective sandbox sync daemon inspired by guploadsync architecture"
license = "MIT"

[[bin]]
name = "guploadsync"
path = "src/main.rs"

[dependencies]
anyhow = "1.0"
async-trait = "0.1"
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
clap = { version = "4.5", features = ["derive"] }
dirs = "6.0"
google-drive3 = { version = "7.0.0+20251218", default-features = false, features = ["yup-oauth2", "ring"] }
http-body = "1"
http-body-util = "0.1"
hyper = "1"
hyper-rustls = { version = "0.27", default-features = false, features = ["ring"] }
hyper-util = "0.1"
md5 = "0.7"
mime = "0.3"
notify = "6.1"
rusqlite = { version = "0.32", features = ["bundled"] }
serde = { version = "1.0", features = ["derive"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal", "sync", "time"] }
tokio-util = "0.7"
toml = "0.8"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }
trash = "5.2"
uuid = { version = "1", features = ["v5"] }
walkdir = "2.5"
yup-oauth2 = "12"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
